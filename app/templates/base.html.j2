<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}DILWL Staff Portal - Free. Cross-platform. Open-source{% endblock %}</title>
    
    <!-- Bootstrap 5 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <!-- Font Awesome 6 -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">

    <style>
        body {
            font-family: 'Inter', sans-serif;
            padding-top: 70px;
            background-color: #f8f9fa;
        }
        .navbar-dark.bg-dark {
            background-color: #1e1e1e !important;
        }
        .navbar-brand {
            font-weight: 700;
            font-size: 1.6rem;
        }
        .navbar-brand i {
            color: #5c2d91;
        }
        .btn-purple {
            background-color: #5c2d91;
            border-color: #5c2d91;
            color: white;
        }
        .btn-purple:hover {
            background-color: #4a2373;
            border-color: #4a2373;
        }
        .btn-outline-purple {
            border-color: #5c2d91;
            color: #5c2d91;
        }
        .btn-outline-purple:hover {
            background-color: #5c2d91;
            color: white;
        }
        .hero {
            background: linear-gradient(135deg, #5c2d91 0%, #7b4cb6 100%);
            color: white;
            padding: 5rem 0;
            margin-bottom: 3rem;
            border-radius: 0 0 2rem 2rem;
        }

        /* ==================== Sticky Footer ==================== */
        html, body {
            height: 100%;
            margin: 0;
        }
        .main-content {
            min-height: calc(100vh - 70px - 230px);
            padding-bottom: 2rem;
        }
        .footer {
            background-color: #1e1e1e;
            color: #aaa;
            padding: 3rem 0;
            margin-top: auto;
            width: 100%;
        }
        .footer a {
            color: #ccc;
            text-decoration: none;
        }
        .footer a:hover {
            color: white;
        }
        .footer h5, .footer h6 {
            color: #ddd;
        }
    </style>

    {% block extra_css %}{% endblock %}
</head>
<body>
    <!-- Navigation Bar -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
        <div class="container">
            <a class="navbar-brand" href="{{ url_for('main.index') }}">
                <i class="fab fa-microsoft"></i> DILWL Staff Portal
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item"><a class="nav-link" href="{{ url_for('tutorials.list') }}">Tutorials</a></li>
                    <li class="nav-item"><a class="nav-link" href="{{ url_for('resources.list') }}">Resources</a></li>
                    <li class="nav-item"><a class="nav-link" href="{{ url_for('community.index') }}">Staff Hub</a></li>
                    {% if current_user.is_authenticated and current_user.is_admin() %}
                    <li class="nav-item"><a class="nav-link" href="{{ url_for('admin.dashboard') }}">Admin</a></li>
                    {% endif %}
                </ul>
                <ul class="navbar-nav">
                    {% if current_user.is_authenticated %}
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" href="#" data-bs-toggle="dropdown">
                                <i class="fas fa-user-circle"></i> {{ current_user.username }}
                            </a>
                            <ul class="dropdown-menu dropdown-menu-end">
                                <li><a class="dropdown-item" href="{{ url_for('main.user_profile', username=current_user.username) }}">Profile</a></li>
                                <li><hr class="dropdown-divider"></li>
                                <li><a class="dropdown-item" href="{{ url_for('auth.logout') }}">Logout</a></li>
                                <li><a class="dropdown-item" href="{{ url_for('tutorials.list_categories') }}">Manage Categories</a></li>
                                <li><a class="dropdown-item" href="{{ url_for('tutorials.list_tags') }}">Manage Tags</a></li>
                                <li><a class="dropdown-item" href="{{ url_for('tutorials.create') }}">New Tutorial</a></li>
                            </ul>
                        </li>
                    {% else %}
                        <li class="nav-item"><a class="nav-link" href="{{ url_for('auth.login') }}">Login</a></li>
                        <li class="nav-item"><a class="nav-link" href="{{ url_for('auth.register') }}">Register</a></li>
                    {% endif %}
                </ul>
            </div>
        </div>
    </nav>

    <!-- Main Content -->
    <div class="main-content">
        <!-- Flash Messages -->
        <div class="container mt-3">
            {% with messages = get_flashed_messages(with_categories=true) %}
                {% if messages %}
                    {% for category, message in messages %}
                        <div class="alert alert-{{ category }} alert-dismissible fade show" role="alert">
                            {{ message }}
                            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                        </div>
                    {% endfor %}
                {% endif %}
            {% endwith %}
        </div>

        <main>
            {% block content %}{% endblock %}
        </main>
    </div>

    <!-- Footer -->
    <footer class="footer">
        <div class="container">
            <div class="row">
                <div class="col-md-4 mb-3">
                    <h5>DILWL Staff Portal</h5>
                    <p class="text-muted">Secure • Efficient • For DILWL Staff<br>Build web apps and services with .NET and C#.</p>
                </div>
                <div class="col-md-2 mb-3">
                    <h6>Learn</h6>
                    <ul class="list-unstyled">
                        <li><a href="{{ url_for('tutorials.list') }}">Tutorials</a></li>
                        <li><a href="#">Documentation</a></li>
                        <li><a href="#">Samples</a></li>
                    </ul>
                </div>
                <div class="col-md-2 mb-3">
                    <h6>Staff Hub</h6>
                    <ul class="list-unstyled">
                        <li><a href="{{ url_for('community.index') }}">Forum</a></li>
                        <li><a href="#">GitHub</a></li>
                        <li><a href="#">Stack Overflow</a></li>
                    </ul>
                </div>
                <div class="col-md-4 mb-3">
                    <h6>Follow Us</h6>
                    <a href="#" class="text-muted me-3"><i class="fab fa-twitter fa-lg"></i></a>
                    <a href="#" class="text-muted me-3"><i class="fab fa-github fa-lg"></i></a>
                    <a href="#" class="text-muted"><i class="fab fa-linkedin fa-lg"></i></a>
                </div>
            </div>
            <hr>
            <div class="text-center text-muted small">
                &copy; 2026 DILWL Staff Internal System | Rebuilt with Flask
            </div>
        </div>
    </footer>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>

    {% block extra_js %}{% endblock %}
</body>
</html>
